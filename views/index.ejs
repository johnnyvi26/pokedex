<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>INDEX</title>
</head>
<body>
    <h1>POKEMONS</h1>
    <ul>
        <% for(let i = 0; i < pokemons.length; i++) { %>
            <li>

                <a href="/pokemon/<%= i %>">
                    <img src="<%= pokemons[i].img%>">
                </a>
                <form action="/pokemon/<%= i %>?_method=DELETE" method="POST" style="display: inline; margin-left: 15px;">
                    <input type="submit" value="DELETE" />
                </form>
                <a href="/pokemon/<%= i %>/edit">Edit</a>

            </li>
        <% } %>
    </ul>
</body>
</html>